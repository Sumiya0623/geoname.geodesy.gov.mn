import { NumberListView } from "src/sections/number/view";
export const metadata = {
  title: "Дашбоард: Дугаарлалтын жагсаалт",
};
export default function NumberListPage() {
  return <NumberListView />;
}
